async function custom_keyword(pageUrl, page, logger, playwright) {
    // logger("inside custom_keyword");
    await page.goto(pageUrl);
    return await page.title();
}
exports.__esModule = true;
exports.custom_keyword = custom_keyword;
