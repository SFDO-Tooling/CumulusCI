Update Accounts:
    sf_object: Account
    table: accounts
    action: update
    fields:
        Id: sf_id
        Name: Name
    lookups:
        parentid:
            table: accounts
            key_field: parent_id
            name: ParentId
